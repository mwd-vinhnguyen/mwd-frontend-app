// import React from 'react';
// import { shallow } from 'enzyme';

// import { {{ properCase component }} } from '../{{ properCase component }}';

describe('<{{ properCase component }} />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
